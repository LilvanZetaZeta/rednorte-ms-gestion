server:
  port: 8081

spring:
  application:
    name: ms-gestion
  datasource:
    # 1. Le agregamos 'jdbc:' al principio y quitamos el usuario/password de la URL
    # Cambiamos pgbouncer=true por prepareThreshold=0 (Requisito estricto de Java con Supabase)
    url: jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
    
    # 2. El usuario va acá
    username: postgres.eqmboygugsouuzoezpvh
    
    # 3. Tu contraseña real
    password: rednorte1234.
    
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: KoLoimwMui9+TcGommo2TlG+GkK+uOmaQPthE1odUOonwdqf4XcyaahSagJI7TSwbw7BI9vvj6+oIUpkBzAyJg==

management:
  endpoints:
    web:
      exposure:
        include: health