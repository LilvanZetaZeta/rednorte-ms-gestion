services:
  app:
    build: .
    container_name: ms-registro-app
    ports:
      - "8081:8081"
    env_file:
      - .env
    networks:
      - network-RedNorte

networks:
  network-RedNorte:
    external: true
